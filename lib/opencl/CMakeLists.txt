
#set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -DDEHANCER_GPU_OPENCL=1")
#set(CMAKE_CXX_FLAGS "${CMAKE_C_FLAGS} -DDEHANCER_GPU_OPENCL=1")

add_definitions(-DDEHANCER_GPU_OPENCL=1)

set (PROJECT_LIB_PLATFORM_SUFFIX _opencl)
set (PROJECT_LIB_PLATFORM ${PROJECT_LIB}${PROJECT_LIB_PLATFORM_SUFFIX})

file (GLOB COMMON_IMPL_SRC ${COMMON_IMPL_SRC}
        ${CMAKE_SOURCE_DIR}/src/platforms/opencl/*.cpp
        )

include(${CMAKE_SOURCE_DIR}/lib/CMake.in)
